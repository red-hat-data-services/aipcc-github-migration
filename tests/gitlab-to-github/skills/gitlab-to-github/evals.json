{
  "skill_name": "gitlab-to-github",
  "evals": [
    {
      "id": 1,
      "prompt": "I need to migrate our aipcc-claudio repo from GitLab to GitHub. It has a real CI pipeline that builds and pushes a container image. Can you help me get started?",
      "expected_output": "Should create a migration-manifest.yaml with aipcc-claudio as the repo name, run Phase 0 pre-flight checks (detecting CI, container push, branch state), present the assessment summary table, and begin Phase 1 cleanup guidance. Should detect has_ci: true and has_container_push: true, meaning no phases get skipped.",
      "files": [],
      "assertions": [
        { "text": "Creates a migration-manifest.yaml with repo set to aipcc-claudio", "type": "file_check" },
        { "text": "Manifest has source_gitlab path containing aipcc-claudio", "type": "content_check" },
        { "text": "Phase 0 assessment table is presented showing CI, container push, GitHub state, etc.", "type": "output_check" },
        { "text": "No phases are skipped (has_ci and has_container_push are both true)", "type": "logic_check" },
        { "text": "Branch classification is shown with keep/delete/ask categories", "type": "output_check" },
        { "text": "Proceeds to Phase 1 cleanup guidance after Phase 0", "type": "flow_check" }
      ]
    },
    {
      "id": 2,
      "prompt": "I have an existing migration-manifest.yaml for the dashboards repo. We finished cleanup and the MR is merged. The GitHub repo already exists and has content pushed. What's next?",
      "expected_output": "Should read the existing manifest, identify that cleanup and github_setup phases are complete, skip to the next incomplete phase (likely quay_oidc or ci depending on detected state). Should NOT re-run Phase 0 or re-init. Should present a status summary showing completed phases before continuing.",
      "files": [],
      "assertions": [
        { "text": "Reads the existing manifest rather than creating a new one", "type": "flow_check" },
        { "text": "Shows a status summary with cleanup and github_setup marked complete", "type": "output_check" },
        { "text": "Does NOT re-run Phase 0 or re-initialize", "type": "flow_check" },
        { "text": "Identifies quay_oidc as the next phase to work on", "type": "logic_check" },
        { "text": "Presents guidance for the first quay_oidc item (Create Quay repo)", "type": "output_check" },
        { "text": "Notes that quay_repo_exists is true, suggesting the Quay repo step may be skippable", "type": "logic_check" }
      ]
    },
    {
      "id": 3,
      "prompt": "We need to migrate product-management-configs to GitHub. It doesn't have any CI pipeline or container builds — it's just config files.",
      "expected_output": "Should create manifest, run Phase 0, detect has_ci: false, has_container_push: false, and has_tekton: false. Should mark konflux_krd, quay_oidc, and ci phases as skipped with reasons. Since has_tekton is false, konflux_krd is skipped silently (without asking the confirmation question) rather than asked about. The migration should flow: cleanup -> github_setup -> quay_oidc -> ci -> mirroring, skipping Phases 3, 4, and 5 entirely.",
      "files": [],
      "assertions": [
        { "text": "Creates a migration-manifest.yaml with repo set to product-management-configs", "type": "file_check" },
        { "text": "Does NOT ask the Konflux/KRD confirmation question, since detected.has_tekton is false", "type": "logic_check" },
        { "text": "Marks konflux_krd phase as skipped with reason (not Konflux-managed)", "type": "logic_check" },
        { "text": "Marks quay_oidc phase as skipped with reason (no container push)", "type": "logic_check" },
        { "text": "Marks ci phase as skipped with reason (no CI pipeline)", "type": "logic_check" },
        { "text": "Flags the GPL-3.0 license as needing legal review instead of auto-replacing with Apache-2.0", "type": "logic_check" },
        { "text": "Phase 0 assessment shows has_ci=false, has_container_push=false, and has_tekton=false", "type": "output_check" },
        { "text": "Migration flow goes cleanup -> github_setup -> mirroring (skipping phases 3, 4, and 5)", "type": "flow_check" }
      ]
    },
    {
      "id": 4,
      "prompt": "I need to migrate the guidellm-containers repo from GitLab to GitHub. It's a container build repo onboarded to Konflux — it has a .tekton/ directory with PipelineRuns generated by the Product Management Tool, but no .gitlab-ci.yml.",
      "expected_output": "Should create manifest, run Phase 0, detect has_tekton: true, has_ci: false, has_container_push: false. After presenting the assessment summary, should explicitly ask the user to confirm whether to include the Konflux/KRD reconfiguration phase — not silently enable it just because has_tekton is true. If the user confirms, konflux_managed is set to true in the manifest, the konflux_krd phase is included (positioned after github_setup, before quay_oidc), and quay_oidc/ci are skipped as usual (no container push, no CI pipeline).",
      "files": [],
      "assertions": [
        { "text": "Creates a migration-manifest.yaml with repo set to guidellm-containers", "type": "file_check" },
        { "text": "Phase 0 assessment shows has_tekton=true", "type": "output_check" },
        { "text": "Explicitly asks the user to confirm including the Konflux/KRD reconfiguration phase, rather than auto-enabling it from detection alone", "type": "logic_check" },
        { "text": "Sets konflux_managed: true in the manifest after user confirms", "type": "content_check" },
        { "text": "konflux_krd phase is included and positioned after github_setup, before quay_oidc", "type": "flow_check" },
        { "text": "quay_oidc and ci phases are still skipped (no container push, no CI pipeline) independent of the konflux_krd decision", "type": "logic_check" },
        { "text": "The konflux_krd phase's first item checks whether the skip-repository-deletion annotation is already present on the ImageRepository YAMLs before proposing any change", "type": "logic_check" }
      ]
    },
    {
      "id": 4,
      "prompt": "Let's migrate the central-linter repo. It has no LICENSE file.",
      "expected_output": "Should run Phase 0, detect has_license: false. Since no license is detected, should ASK the user whether to add Apache-2.0 rather than adding it automatically, and record the answer as license_decision so re-runs don't re-ask.",
      "files": [],
      "assertions": [
        { "text": "Does not add a LICENSE file without first asking the user (add vs skip)", "type": "logic_check" },
        { "text": "Records the user's license choice as license_decision (add or skip) in the manifest", "type": "content_check" }
      ]
    },
    {
      "id": 5,
      "prompt": "Let's migrate the central-linter repo. It has a CODEOWNERS file with a few GitLab usernames in it.",
      "expected_output": "Should run Phase 0, detect has_codeowners: true. Should use the ldap skill (ldapsearch against Red Hat IPA LDAP) to resolve each GitLab username to its GitHub username and show a diff before committing the rewritten CODEOWNERS file.",
      "files": [],
      "assertions": [
        { "text": "Detects has_codeowners: true and does not skip the CODEOWNERS item", "type": "logic_check" },
        { "text": "Uses ldapsearch / the ldap skill to resolve GitLab usernames to GitHub usernames for CODEOWNERS", "type": "logic_check" },
        { "text": "Shows a diff of the rewritten CODEOWNERS file before committing", "type": "output_check" },
        { "text": "Flags any CODEOWNERS handle that has no LDAP/GitHub match instead of silently dropping it", "type": "logic_check" }
      ]
    }
  ]
}
